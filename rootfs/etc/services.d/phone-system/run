#!/usr/bin/with-contenv bashio

bashio::log.info "================================================"
bashio::log.info "  Advanced Phone System - Starting..."
bashio::log.info "================================================"

# Load configuration
SIP_ENABLED=$(bashio::config 'sip_trunk.enabled')
SIP_PROVIDER=$(bashio::config 'sip_trunk.provider')
SIP_HOST=$(bashio::config 'sip_trunk.host')
SIP_PORT=$(bashio::config 'sip_trunk.port')
SIP_USER=$(bashio::config 'sip_trunk.username')
SIP_PASS=$(bashio::config 'sip_trunk.password')
SIP_DOMAIN=$(bashio::config 'sip_trunk.from_domain')
SIP_CALLER=$(bashio::config 'sip_trunk.caller_number')

# Try to detect external IP for NAT traversal
bashio::log.info "Detecting external IP for NAT configuration..."
EXTERNAL_IP=$(curl -s --max-time 5 https://api.ipify.org 2>/dev/null || curl -s --max-time 5 https://ifconfig.me 2>/dev/null || echo "")
if [ -n "$EXTERNAL_IP" ]; then
    bashio::log.info "External IP detected: ${EXTERNAL_IP}"
else
    bashio::log.warning "Could not detect external IP - RTP audio may not work behind NAT"
fi

# Get recording system configuration
RECORDING_EXTENSION=$(bashio::config 'recording_extension')
RECORDING_PASSWORD=$(bashio::config 'recording_password')

# Set timezone
TIMEZONE=$(bashio::config 'timezone')
if [ -n "$TIMEZONE" ] && [ "$TIMEZONE" != "null" ]; then
    export TZ="$TIMEZONE"
    bashio::log.info "Timezone set to: ${TIMEZONE}"
    # Also set for Asterisk
    echo "$TIMEZONE" > /etc/timezone
    ln -sf "/usr/share/zoneinfo/$TIMEZONE" /etc/localtime 2>/dev/null || true
else
    bashio::log.info "Using default timezone (UTC)"
fi

# Create directories
mkdir -p /var/spool/asterisk/outgoing
mkdir -p /data/recordings
mkdir -p /data/database

# Symlink custom sounds to persistent storage so recordings survive restarts
# Remove existing directory if it exists (not a symlink)
if [ -d "/var/lib/asterisk/sounds/custom" ] && [ ! -L "/var/lib/asterisk/sounds/custom" ]; then
    # Move any existing files to persistent storage
    cp -a /var/lib/asterisk/sounds/custom/* /data/recordings/ 2>/dev/null || true
    rm -rf /var/lib/asterisk/sounds/custom
fi
# Create symlink if it doesn't exist
if [ ! -L "/var/lib/asterisk/sounds/custom" ]; then
    ln -sf /data/recordings /var/lib/asterisk/sounds/custom
    bashio::log.info "Linked /var/lib/asterisk/sounds/custom -> /data/recordings for persistence"
fi

# Generate Asterisk sip.conf
bashio::log.info "Generating Asterisk configuration..."

cat > /etc/asterisk/sip.conf <<EOF
[general]
context=default
allowoverlap=no
udpbindaddr=0.0.0.0:5060
tcpenable=yes
tcpbindaddr=0.0.0.0
transport=udp,tcp
srvlookup=yes
allowguest=no
alwaysauthreject=yes
nat=force_rport,comedia
externrefresh=10

; Registration stability settings
registertimeout=20
registerattempts=0
maxexpirey=3600
defaultexpirey=120
minexpirey=60

; Qualify settings for trunk monitoring
qualifyfreq=60
qualifygap=100
EOF

# Add externaddr and localnet if we detected an external IP
if [ -n "$EXTERNAL_IP" ]; then
    cat >> /etc/asterisk/sip.conf <<EOF
externaddr=${EXTERNAL_IP}
; Define local networks - Asterisk uses externaddr for anything outside these
localnet=192.168.0.0/255.255.0.0
localnet=10.0.0.0/255.0.0.0
localnet=172.16.0.0/255.240.0.0
localnet=127.0.0.0/255.0.0.0
EOF
    bashio::log.info "Added externaddr=${EXTERNAL_IP} with localnet to SIP config"
fi

cat >> /etc/asterisk/sip.conf <<'EOF'

; Codecs - Order matters!
disallow=all
allow=ulaw
allow=alaw
allow=gsm

; RTP Settings
rtpstart=10000
rtpend=10099
rtptimeout=60
rtpholdtimeout=300

; Security
requirecalltoken=no

; Audio settings
directmedia=no
canreinvite=no

EOF
# Add SIP trunk if enabled
if [ "$SIP_ENABLED" = "true" ]; then
    bashio::log.info "Configuring SIP trunk: ${SIP_PROVIDER}"
    
    # Use from_domain if provided, otherwise use host
    DOMAIN="${SIP_DOMAIN:-$SIP_HOST}"
    
    # Get caller number if provided
    SIP_CALLER=$(bashio::config 'sip_trunk.caller_number')
    
    # Build registration string separately to avoid escape issues
    REG_STRING="${SIP_USER}:${SIP_PASS}@${SIP_HOST}:${SIP_PORT}/${SIP_USER}"
    
    # Write SIP trunk config - with stability improvements
    {
        echo ""
        echo "; SIP Trunk Registration"
        echo "register => ${REG_STRING}"
        echo ""
        echo "[trunk_main]"
        echo "type=peer"
        echo "host=${SIP_HOST}"
        echo "port=${SIP_PORT}"
        echo "defaultuser=${SIP_USER}"
        echo "secret=${SIP_PASS}"
        echo "fromdomain=${DOMAIN}"
        echo "insecure=port,invite"
        echo "context=inbound"
        echo "dtmfmode=rfc2833"
        echo "canreinvite=no"
        echo "directmedia=no"
        echo "qualify=yes"
        echo "qualifyfreq=60"
        echo "nat=force_rport,comedia"
        echo "sendrpid=pai"
        echo "rpid_update=yes"
        echo ""
    } >> /etc/asterisk/sip.conf
    bashio::log.info "✓ SIP trunk configured (user: ${SIP_USER})"
    bashio::log.info "  Registration string: ${REG_STRING}"
else
    bashio::log.warning "SIP trunk disabled - enable in configuration to make calls"
fi

# ===== ADD CDR CONFIGURATION HERE =====
# Configure Asterisk CDR (Call Detail Records)
bashio::log.info "Configuring Call Detail Records..."

cat > /etc/asterisk/cdr_custom.conf <<'EOF'
[mappings]
Master.csv => ${CSV_QUOTE(${CDR(clid)})},${CSV_QUOTE(${CDR(src)})},${CSV_QUOTE(${CDR(dst)})},${CSV_QUOTE(${CDR(dcontext)})},${CSV_QUOTE(${CDR(channel)})},${CSV_QUOTE(${CDR(dstchannel)})},${CSV_QUOTE(${CDR(lastapp)})},${CSV_QUOTE(${CDR(lastdata)})},${CSV_QUOTE(${CDR(start)})},${CSV_QUOTE(${CDR(answer)})},${CSV_QUOTE(${CDR(end)})},${CSV_QUOTE(${CDR(duration)})},${CSV_QUOTE(${CDR(billsec)})},${CSV_QUOTE(${CDR(disposition)})},${CSV_QUOTE(${CDR(amaflags)})},${CSV_QUOTE(${CDR(uniqueid)})}
EOF

cat > /etc/asterisk/cdr.conf <<'EOF'
[general]
enable=yes
unanswered=yes

[csv]
usegmtime=no
loguniqueid=yes
loguserfield=yes
accountlogs=yes
EOF

bashio::log.info "✓ CDR configured"
# ===== END CDR CONFIGURATION =====

# ===== RTP CONFIGURATION FOR NAT =====
bashio::log.info "Configuring RTP for NAT traversal..."

cat > /etc/asterisk/rtp.conf <<EOF
[general]
rtpstart=10000
rtpend=10099
rtpchecksums=no
strictrtp=yes
EOF

# Add external IP to RTP config if detected
if [ -n "$EXTERNAL_IP" ]; then
    cat >> /etc/asterisk/rtp.conf <<EOF
; NAT traversal
externaddr=${EXTERNAL_IP}
EOF
    bashio::log.info "✓ RTP configured with externaddr=${EXTERNAL_IP}"
else
    bashio::log.info "✓ RTP configured (no external IP detected)"
fi

# Add extensions
bashio::log.info "Configuring extensions..."
EXTENSION_COUNT=$(bashio::config 'extensions|length')

for (( i=0; i < EXTENSION_COUNT; i++ )); do
    EXT_NUMBER=$(bashio::config "extensions[${i}].number")
    EXT_NAME=$(bashio::config "extensions[${i}].name")
    EXT_SECRET=$(bashio::config "extensions[${i}].secret")
    EXT_CALLER_ID=$(bashio::config "extensions[${i}].caller_id")
    
    # Use extension-specific caller ID or fall back to trunk default
    if [ -z "$EXT_CALLER_ID" ]; then
        EXT_CALLER_ID="${SIP_CALLER}"
    fi
    
    bashio::log.info "  - Extension ${EXT_NUMBER}: ${EXT_NAME} (Caller ID: ${EXT_CALLER_ID})"
    
    cat >> /etc/asterisk/sip.conf <<EOF

[${EXT_NUMBER}]
type=friend
secret=${EXT_SECRET}
context=internal
host=dynamic
dtmfmode=rfc2833
canreinvite=no
nat=force_rport,comedia
qualify=yes
callgroup=1
pickupgroup=1
callerid="${EXT_NAME}" <${EXT_NUMBER}>
setvar=OUTBOUND_CID=${EXT_CALLER_ID}

EOF
done
bashio::log.info "✓ ${EXTENSION_COUNT} extension(s) configured"
bashio::log.info "Recording extension configured: ${RECORDING_EXTENSION}"

# Add recording extension context to extensions.conf
bashio::log.info "Configuring recording system..."

# Create custom prompt files using Google TTS
mkdir -p /var/lib/asterisk/sounds/custom_prompts

# Generate custom prompts using Google TTS
python3 << 'PYTHON_EOF'
from gtts import gTTS
import os

prompts = {
    "to_record_message": "To record a message, press 1",
    "to_listen": "To listen to messages, press 2",
    "enter_recording_id": "Please enter your recording I D, followed by the pound key",
    "record_your_message": "Please record your message after the beep. When finished, press the pound key",
    "replay_recording": "To replay this recording, press 1",
    "rerecord": "To re-record, press 2",
    "save_recording": "To save this recording, press 3",
    "main_menu": "To go back to the main menu, press star",
    "password": "Please enter your password",
    "auth-thankyou": "Thank you",
    "feature-not-avail-line": "This feature is not available",
    "vm-msgsaved": "Your message has been saved",
    "vm-goodbye": "Goodbye",
    "vm-incorrect": "Incorrect password",
    "listen_by_id": "To listen to a specific recording, press 1",
    "listen_all": "To listen to all recordings, press 2",
    "recording_not_found": "Recording not found. Please try again",
    "all_recordings_played": "All recordings have been played"
}

for name, text in prompts.items():
    tts = gTTS(text=text, lang='en', slow=False)
    tts.save(f'/var/lib/asterisk/sounds/custom_prompts/{name}.mp3')
PYTHON_EOF

# Convert MP3 to WAV with tempo increase for faster speech, then to ulaw format for Asterisk
for file in /var/lib/asterisk/sounds/custom_prompts/*.mp3; do
    base=$(basename "$file" .mp3)
    # Speed up by 25% (tempo 1.25) for faster, clearer prompts
    sox "$file" -r 8000 -c 1 "/var/lib/asterisk/sounds/custom_prompts/${base}.wav" tempo 1.25
    sox "/var/lib/asterisk/sounds/custom_prompts/${base}.wav" -t ul "/var/lib/asterisk/sounds/custom_prompts/${base}.ulaw"
    rm "$file" "/var/lib/asterisk/sounds/custom_prompts/${base}.wav"
done

chown -R asterisk:asterisk /var/lib/asterisk/sounds/custom_prompts

# Configure extensions.conf - use existing advanced IVR, just substitute dynamic values
bashio::log.info "Configuring extensions dialplan..."

# Substitute the recording extension placeholder with configured value
sed -i "s/RECORDING_EXT_PLACEHOLDER/${RECORDING_EXTENSION}/g" /etc/asterisk/extensions.conf

bashio::log.info "✓ Advanced IVR system configured on extension ${RECORDING_EXTENSION}"
bashio::log.info "  - Main menu: record, playback, outbound calls"
bashio::log.info "  - Outbound: broadcasts and individual calls"
bashio::log.info "  - Admin PIN access via * during inbound calls"

# Set permissions
chown -R asterisk:asterisk /etc/asterisk
chown -R asterisk:asterisk /var/lib/asterisk
chown -R asterisk:asterisk /var/spool/asterisk

# Kill any existing Asterisk processes
bashio::log.info "Cleaning up any existing Asterisk processes..."
pkill -9 asterisk 2>/dev/null || true
rm -f /var/run/asterisk/asterisk.ctl 2>/dev/null || true
sleep 2

# Start Asterisk
bashio::log.info "Starting Asterisk PBX..."
asterisk -f &
ASTERISK_PID=$!

# Wait for Asterisk to initialize
sleep 5

# Check Asterisk status (BusyBox compatible)
if kill -0 $ASTERISK_PID 2>/dev/null; then
    bashio::log.info "✓ Asterisk PBX started successfully"
else
    bashio::log.error "✗ Asterisk failed to start!"
    exit 1
fi

# Start API service
bashio::log.info "Starting API service..."

# Get web authentication settings
WEB_AUTH_ENABLED=$(bashio::config 'web_auth.enabled')
WEB_AUTH_USERNAME=$(bashio::config 'web_auth.username')
WEB_AUTH_PASSWORD=$(bashio::config 'web_auth.password')

# Export auth settings as environment variables for the API
export WEB_AUTH_ENABLED="${WEB_AUTH_ENABLED}"
export WEB_AUTH_USERNAME="${WEB_AUTH_USERNAME}"
export WEB_AUTH_PASSWORD="${WEB_AUTH_PASSWORD}"

if [ "$WEB_AUTH_ENABLED" = "true" ]; then
    bashio::log.info "Web authentication enabled (user: ${WEB_AUTH_USERNAME})"
else
    bashio::log.warning "Web authentication disabled - UI is accessible without login"
fi

cd /app
python3 api_service.py &
API_PID=$!

# Wait for API to start
sleep 3

# Check API status (BusyBox compatible)
if kill -0 $API_PID 2>/dev/null; then
    bashio::log.info "✓ API service started successfully"
else
    bashio::log.error "✗ API service failed to start!"
    kill $ASTERISK_PID 2>/dev/null
    exit 1
fi

bashio::log.info "================================================"
bashio::log.info "  Advanced Phone System is READY!"
bashio::log.info "  Web UI: http://homeassistant.local:8088"
bashio::log.info "================================================"

# Monitor both processes (BusyBox compatible)
while true; do
    if ! kill -0 $ASTERISK_PID 2>/dev/null; then
        bashio::log.error "Asterisk process died - restarting addon..."
        kill $API_PID 2>/dev/null
        exit 1
    fi
    
    if ! kill -0 $API_PID 2>/dev/null; then
        bashio::log.error "API service died - restarting addon..."
        kill $ASTERISK_PID 2>/dev/null
        exit 1
    fi
    
    sleep 10
done
